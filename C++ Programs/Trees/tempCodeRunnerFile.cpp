else
            // {
            //     Node *temp = minimum(node->right);
            //     node->data = temp->data;
            //     node->right = deleteHelper(node->right, temp->data);
            // }