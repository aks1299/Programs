#include <stdio.h>

int main()
{
    // int arr[100];
    // int j = 0, pos = 3;
    // for (int i = 0; i < 5; i++)
    // {
    //     printf("Enter the element : ");
    //     scanf("%d", &arr[i]);
    // }
    // // Insertion of an element at a specific position in array using Shift method
    // for (int i = 4; i >= pos; i--)
    // {
    //     arr[i + 1] = arr[i];
    // }
    // arr[pos] = 99;
    // for (int i = 0; i < 61; i++)
    // {
    //     printf("%d ", arr[i]);
    // }
    printf("%d", 0 && 1);
}