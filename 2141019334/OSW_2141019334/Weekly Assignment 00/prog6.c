#include <stdio.h>

int main(int argc, char const *argv[])
{
    float len,area;
    printf("Enter the side of the square : ");
    scanf("%f",&len);
    printf("The side of the square is : %f and its area is : %f",len,len*len);
    return 0;
}
